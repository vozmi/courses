/**
 * Converts number from
 * @param phoneNumber
 * @returns true if the passed string looks like a valid US phone number, false if don't
 */
export function telephoneCheck(phoneNumber: string): string {
    throw 'Not implemented!';
}
